/// This class describes Enum type declared in manifest.

class FieldType {
    filename;
    name;

    constructor(filename, name) {
        this.filename = filename;
        this.name = name;
    }
}

export {FieldType};